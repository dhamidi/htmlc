The task list has been written to `.factory/tasks.md`. Here's a summary of the 15 ordered implementation tasks:

1. **Set up Go module and package skeleton** — `go.mod` and all empty package files
2. **Implement expression AST node types** — `expr/ast.go` with all node types and `Expr` interface
3. **Implement expression lexer** — `expr/lexer.go` tokeniser for JS-compatible expressions
4. **Implement expression parser** — `expr/parser.go` recursive-descent parser with correct precedence
5. **Implement expression evaluator** — `expr/eval.go` with JS-style semantics and Go interop
6. **Implement SFC parser** — `component.go` to extract template/script/style from `.vue` files
7. **Template renderer — interpolation and v-text/v-html** — `renderer.go` core with mustache and text directives
8. **v-if / v-else-if / v-else directives** — Conditional rendering with `<template>` support
9. **v-for directive** — List rendering for arrays, maps, and integers
10. **Attribute binding (v-bind / :attr)** — Dynamic attrs, class/style merging, boolean attributes
11. **v-show, v-pre, v-once, and pass-through directives** — Remaining simple directives
12. **Style scoping** — FNV-1a scope IDs, CSS rule rewriting, style collection
13. **Component composition and slots** — Recursive component rendering and `<slot />`
14. **Engine with registry, auto-discovery, and reload** — `engine.go` with full public API
15. **End-to-end integration tests** — Full pipeline tests including HTTP handler